import 'package:flutter/material.dart';

class HrLine extends StatelessWidget {
  const HrLine({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 1,
      width: double.infinity,
      color: Colors.orangeAccent,
    );
  }
}
